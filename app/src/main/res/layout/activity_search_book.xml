<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:padding="16dp">

    <EditText
        android:id="@+id/etSearchBook"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Cari buku..."
        android:padding="12dp"
        android:background="@drawable/bg_form_daftar"
        android:inputType="text"
        android:contentDescription="Input pencarian buku" />

    <Button
        android:id="@+id/btnSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cari"
        android:layout_marginTop="12dp"
        android:backgroundTint="#016CF8"
        android:textColor="@android:color/white"
        android:contentDescription="Tombol cari buku" />

    <TextView
        android:id="@+id/tvResult"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:layout_marginTop="16dp"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:contentDescription="Hasil pencarian buku" />

</LinearLayout>
