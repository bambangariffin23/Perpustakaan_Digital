<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Perpustakaandigital">

        <!-- MAIN ACTIVITY (HALAMAN PERTAMA) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ACTIVITY LAPRAK -->
        <activity
            android:name=".Laprak"
            android:exported="false" />

        <!-- ACTIVITY HASIL KERJAMU -->
        <activity android:name=".BookByCategoryActivity" />
        <activity android:name=".BorrowBookActivity" />
        <activity android:name=".HistoryBorrowActivity" />
        <activity android:name=".CategoryBookActivity" />

    </application>

</manifest>
